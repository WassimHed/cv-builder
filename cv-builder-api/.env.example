# App
PORT=3000

# MySQL (matches root docker-compose.yml)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=cv_builder_user
DB_PASSWORD=changeme
DB_DATABASE=cv_builder

JWT_SECRET=changeme_in_dev
JWT_EXPIRATION_TIME=1d

MONGODB_URI=changeme

MINIO_ENDPOINT=localhost
MINIO_API_PORT=9002
MINIO_ROOT_USER=<same as root .env>
MINIO_ROOT_PASSWORD=<same as root .env>
MINIO_BUCKET=cv-builder
LOCAL_STORAGE_PATH=./storage-fallback

BULL_REDIS_HOST=localhost
BULL_REDIS_PORT=6379

GEMINI_API_KEY=changeme
LANGUAGETOOL_URL=https://api.languagetool.org/v2/check

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@cv-builder.local  